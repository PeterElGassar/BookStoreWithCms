@model IEnumerable<E_BookStore.Models.Models.Book>
@{
    ViewData["Title"] = "Home Page";
}


@*@foreach (var product in Model)
{

    <div class="col-lg-3 col-md-6">
            <div class="row p-2">
                <div class="col-12  p-1" style="border:1px solid #008cba; border-radius: 5px;">
                    <div class="card" style="border:0px;">
                        <img src="@product.ImageUrl" class="card-img-top rounded img-fluid" />
                        <div class="pl-1">
                            <p class="card-title h5"><b style="color:#2c3e50">@product.Title</b></p>
                            <p class="card-title text-primary">by <b>@product.Author</b></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p>List Price: <strike><b class="">$@product.ListPrice.ToString("0.00")</b></strike></p>
                        </div>
                        <div style="padding-left:5px;">
                            <p style="color:maroon">As low as: <b class="">$@product.Price100.ToString("0.00")</b></p>
                        </div>
                    </div>
                    <div>
                        <a asp-action="Details" class="btn btn-primary form-control" asp-route-id="@product.Slug">Details</a>
                    </div>
                </div>
            </div>
        </div>
}*@

<section>



    <div class="p-5  book-wrapper">
        @foreach (var product in Model)
        {
            <div class="col-xl-12 rotate pt-3">
                <div class="card  text-center mb-3 book-card">
                    <div class="card-header">
                        <p class="card-title h5"><b style="color:#2c3e50">@product.Title</b></p>
                    </div>
                    <div class="card-body">
                        <img src="@product.ImageUrl" class="card-img-top rounded img-fluid book-thumb" />
                    </div>
                    <div class="card-footer">
                        <h5 class="card-title d-inline">by </h5><b class="Author text-capitalize">@product.Author</b>
                    </div>
                    <div class="back-side">
                        <div class="back-content ">
                            <!---->
                            <div class="pl-1">
                                <p class="card-title h5"><b style="color:#2c3e50">@product.Title</b></p>
                                <p class="card-title text-primary">by <b>@product.Author</b></p>
                            </div>
                            <div style="padding-left:5px;">
                                <p>List Price: <strike><b class="">$@product.ListPrice.ToString("0.00")</b></strike></p>
                            </div>
                            <div style="padding-left:5px;">
                                <p style="color:maroon">As low as: <b class="">$@product.Price100.ToString("0.00")</b></p>
                            </div>
                            <div>
                                <a asp-action="Details" class="btn btn-primary form-control" asp-route-id="@product.Slug">Details</a>
                            </div>

                            <a href="#" class="btn btn-outline-danger mb-2">Buy Now</a>
                        </div>
                    </div>
                </div>
            </div>
        }

        </div>


    

</section>











